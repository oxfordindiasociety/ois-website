div(class='col-sm-12 col-md-6 ' + event.metadata.category)
  if event.metadata.thumb
    .thumbnail
      img(src=event.metadata.thumb)
  .caption
    h3= event.title
    h4= event.metadata.date
    h5.location= event.metadata.location
    p!= event.html
